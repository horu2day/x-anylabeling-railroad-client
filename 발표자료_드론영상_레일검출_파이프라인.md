# 드론 정사영상 기반 레일 자동 검출 파이프라인

> 2cm GSD 드론 정사영상 → 레일 검출 → 중심선 DXF 추출

---

## 1. 프로젝트 개요

### 목적
드론으로 촬영한 고해상도 정사영상에서 **레일 위치를 자동으로 검출**하여
CAD(Rhino 등)에서 활용 가능한 **DXF 중심선** 데이터를 생산

### 활용 방향
- 레일 실제 위치 측량 자동화 (기존 인력 측량 대체)
- 디지털 트윈 구축용 기초 데이터
- 구간별 레일 좌표 추출 (EPSG:5186 세계 좌표)

---

## 2. 사용 데이터

| 항목 | 내용 |
|------|------|
| 영상 종류 | 드론 정사영상 (GeoTIFF) |
| 좌표계 | EPSG:5186 (한국 중부원점) |
| 해상도 | **2cm GSD** (1.78cm/pixel) |
| 대상 구간 | 조치원 STA.127+570 ~ 131+300 |
| 이미지 크기 | 65,766 × 177,192 px (약 11.65 GB) |
| 동일 파일 | 약 40개 구간 추가 예정 |

### 해상도별 레일 폭 비교

| GSD | 레일 폭 (실제 67mm) | 검출 가능 여부 |
|-----|-------------------|--------------|
| 5cm | ~1.4 pixel | 어려움 |
| **2cm** | **~3.5 pixel** | **가능** |

---

## 3. 전체 파이프라인

```
드론 GeoTIFF (2cm GSD)
      ↓
  타일 분할 (1024 × 1024 px, overlap 128px)
      ↓
  X-AnyLabeling + SAM3 라벨링
  (텍스트 프롬프트: "steel rail")
      ↓
  YOLO11n-seg 학습
      ↓
  전체 영상 타일 추론
      ↓
  PCA 중심선 추출 (masks.xy 폴리곤 → 중심선)
      ↓
  픽셀 좌표 → 세계 좌표 변환 (EPSG:5186)
      ↓
  DXF 저장 (Rhino / AutoCAD 호환)
```

---

## 4. 라벨링 방법 — X-AnyLabeling + SAM3

### 구성
- **X-AnyLabeling 3.3.9**: 라벨링 클라이언트
- **SAM3 서버** (localhost:8000): Segment Anything Model 3

### 워크플로우

```
1. 타일 영상(1024px)을 X-AnyLabeling에서 열기
2. 모델 선택: Segment Anything 3
3. 텍스트 프롬프트 입력: "steel rail"
4. 레일 1가닥씩 분할 확인 → 저장 (JSON 자동 생성)
```

### 핵심 포인트
- 레일 **1가닥**을 하나의 세그멘테이션 인스턴스로 라벨링
- 2선 철도 = 타일당 최대 4가닥 검출 가능
- 18장 라벨링 → YOLO 학습에 활용

---

## 5. YOLO 학습 결과

### 모델
- 아키텍처: `YOLO11n-seg` (경량 인스턴스 세그멘테이션)
- 클래스: `steel rail` (1개 클래스)
- 학습 데이터: 2cm GSD 타일 18장

### 성능

| 버전 | 학습 데이터 | Box mAP50 | Mask mAP50 | Recall |
|------|------------|-----------|------------|--------|
| v1 (5cm) | 31장 | 0.770 | 0.495 | 0.895 |
| v1 (2cm) | 5장 | 0.727 | 0.666 | 1.000 |
| **v2 (2cm)** | **18장** | **0.916** | **0.236** | **0.900** |

> Box mAP50 = 0.916: 레일 위치 검출 정확도 높음
> Mask mAP50 = 0.236: 레일 형태(마스크) 품질 개선 여지 있음 → 라벨 추가로 해결 가능

---

## 6. 중심선 추출 방법 — PCA 슬라이싱

### 기존 방법의 문제
- `skeletonize` → 픽셀 단위 정수 좌표 → **계단 현상** 발생
- DXF에서 보면 매끄럽지 않은 계단 형태

### 현재 방법 (PCA 슬라이싱)

```
YOLO masks.xy 폴리곤 (레일 윤곽선)
      ↓
  PCA로 레일 길이 방향 주축 탐색
      ↓
  주축 방향으로 10px 간격 슬라이싱
      ↓
  각 슬라이스에서 폭 방향 기하 중앙값 추출
      ↓
  매끄러운 중심선 (계단 현상 없음)
```

### 정확도
- 샘플 간격: 10px = **약 0.2m**
- 레일 폭(~3.5px) 중앙을 기하학적으로 추출 → 이론 정확도 **±1px ≈ ±2cm**

---

## 7. 처리 결과

### 타일 처리 규모

| 항목 | 수치 |
|------|------|
| 전체 타일 수 | 약 74,000개 |
| 유효 타일 (비검은 영역) | 약 40,000개 |
| 검출 폴리라인 수 | 약 5,200개 |
| 출력 파일 | `rail_centerline_2cm_pca.dxf` |
| DXF 레이어 | `RAIL_CENTERLINE` |

### 좌표 정보
- 좌표계: EPSG:5186 (단위: m)
- Rhino / AutoCAD에서 직접 열기 가능
- 4가닥 레일(2선 철도)이 구분되어 출력

---

## 8. 현재 한계 및 과제

| 항목 | 현황 | 해결 방향 |
|------|------|----------|
| 라벨 데이터 부족 | 18장 | 100장 이상 추가 라벨링 |
| Mask mAP50 낮음 | 0.236 | 라벨 품질·수량 개선 |
| 타일 경계 불연속 | 중심선이 끊김 | 인접 폴리라인 연결 로직 |
| 40개 구간 파일 | 미처리 | 배치 처리 스크립트 |

---

## 9. 다음 단계

1. **라벨 추가** — 다양한 구간/방향 타일 100장 이상 라벨링
2. **모델 고도화** — YOLO11s-seg 또는 YOLO11m-seg 업그레이드
3. **중심선 연결** — 타일 경계 폴리라인 자동 병합
4. **배치 처리** — 40개 TIF 파일 자동화 스크립트

---

## 참고 — 기술 스택

| 구분 | 기술 |
|------|------|
| 라벨링 | X-AnyLabeling 3.3.9 + SAM3 |
| 검출 모델 | YOLO11n-seg (Ultralytics) |
| 영상 처리 | rasterio, Pillow, NumPy |
| 중심선 추출 | NumPy PCA (SVD) |
| DXF 출력 | ezdxf |
| 좌표계 | EPSG:5186 (GeoTIFF 내장) |
| GPU | CUDA (추론 가속) |
