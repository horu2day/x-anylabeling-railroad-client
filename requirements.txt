# X-AnyLabeling Server (.venv) requirements
# Python 3.12+ required
#
# Step 1: Create venv and install PyTorch with CUDA 12.6
#   python -m venv .venv
#   .venv\Scripts\pip.exe install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Step 2: Install this requirements file
#   .venv\Scripts\pip.exe install -r requirements.txt
#
# Step 3: Install X-AnyLabeling-Server
#   .venv\Scripts\pip.exe install -e X-AnyLabeling-Server

# === Server Framework ===
fastapi>=0.128.0
uvicorn>=0.40.0
pydantic>=2.12
pydantic-settings>=2.12
python-multipart>=0.0.22
loguru>=0.7.3

# === ML / Deep Learning ===
# torch and torchvision must be installed separately with CUDA index (see Step 1 above)
triton-windows>=3.6.0
ultralytics>=8.4.9
transformers>=5.0.0
safetensors>=0.7.0
timm>=1.0.24
accelerate>=1.12.0
einops>=0.8.2

# === Image Processing ===
opencv-python-headless>=4.11.0
opencv-contrib-python-headless>=4.11.0
pillow>=12.0.0
scikit-image>=0.26.0
qimage2ndarray>=1.10.0

# === Data / Utils ===
numpy>=2.3
scipy>=1.17.0
pandas>=3.0.0
shapely>=2.1.2
pyclipper>=1.4.0
pycocotools>=2.0.11
tqdm>=4.67
pyyaml>=6.0.3
requests>=2.32
huggingface_hub>=1.3.5

# === OCR / VL Models ===
paddleocr>=3.0.2
paddlepaddle>=2.6.0
qwen-vl-utils>=0.0.14

# === GUI (also used by server for x-anylabeling-cvhub) ===
x-anylabeling-cvhub>=3.3.9
PyQt5>=5.15.11
